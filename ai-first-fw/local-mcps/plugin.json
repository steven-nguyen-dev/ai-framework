{
  "$schema": "https://json.schemastore.org/claude-code-plugin.json",
  "name": "ai-first-fw-mcps",
  "version": "1.0.0",
  "description": "Local stdio MCP servers for the AI-First Framework: jira-reader (issue metadata, ADF descriptions, attachment downloads) and kibana-explorer (enterprise log search via KQL). Packaged as a plugin so hosts that do not read project-scoped .mcp.json — such as Cowork — can load them.",
  "author": {
    "name": "Steven Nguyen"
  },
  "mcpServers": "./.mcp.json"
}
