<!-- HOW TO FILL — Fill top to bottom. Each comment states the shape of the section above it and
ends in a Check: line.
Check: every section below is filled or deleted, and every Check: line is quoted against. -->

---
name: <lower-kebab-case, matching the folder name>
description: <what the skill produces, then one trigger per branch it handles>
version: 0.1.0
disable-model-invocation: <the user's choice: true for user-invoked, false for model-invoked>
---

<!-- Frontmatter — write the description as a pointer: leading word first, one trigger per branch,
the identity the body carries left to the body.
Check: the description names the output and carries one trigger per branch, and
disable-model-invocation holds the user's choice. -->

# <name>

<What this run produces, and where it lands.>

<!-- Opening — write one or two lines, then a pointer line naming each template and reference file
this skill carries and what each one holds.
Check: a cold reader names the deliverable from these lines alone. -->

## Inputs

- **<Input>** — <what counts as this input, and where it comes from>.

<!-- Inputs — write one line per input. Give an input that can arrive absent the move that gets it.
Check: every input the run reads is listed. -->

## Step 1 — <goal>

<The action.>

**Completion:** <the condition a second reader settles by reading the output>

<!-- Steps — write one goal each, in run order, each ending on a criterion that is checkable and
exhaustive: "every X accounted for", counted against the output.
Check: every step ends on a Completion line. -->

## The bar

- <What the finished work satisfies, across the whole run.>

<!-- The bar — write the lines the author quotes the output against at self-check.
Check: every line here is checkable against the output. -->
