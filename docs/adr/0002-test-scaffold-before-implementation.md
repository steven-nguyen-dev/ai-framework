# Executable Test Scaffold Prior to Implementation

We require automated test suites to be authored and validated as failing test scaffolds before implementation code is written. Because LLMs can easily author weak tests that pass trivial implementations, reviewing the executable test assertions first guarantees that invariant boundaries and edge cases are genuinely validated before production code exists.
